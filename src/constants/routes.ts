const home = "/";
const products = "/products";
const cart = "/cart";
const checkOut = "/checkOut";
const social = "/social"

const routes = {
  home,
  products,
  cart,
  checkOut,
  social
};

export default routes;
